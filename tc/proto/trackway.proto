syntax = "proto3";

package trackway;

service Trackway {
  rpc NewSession(stream Message) returns (stream Message) {}
}

message Message {
  enum MessageType {
    MESSAGE_CONTROL = 0;
    MESSAGE_PROMPT = 1;
  }
  MessageType message_type = 1;
  string code = 2;
  bytes data = 3;
}